# Transaction Harvester (ETH / Base / Arbitrum via Alchemy)

### 0) Install
```bash
python -m venv .venv && source .venv/bin/activate   # or Windows equivalent
pip install -r requirements.txt
cp .env.example .env  # add your key
